name: Export master repository ZIP

on:
  push:
    branches:
      - agent/export-master-zip-20260818
    paths:
      - '.github/workflows/export-master-zip.yml'

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master
      - name: Upload master snapshot
        uses: actions/upload-artifact@v4
        with:
          name: short-matthew-f.github.io-master
          path: |
            .
            !.git/**
          include-hidden-files: true
          compression-level: 6
          retention-days: 1
