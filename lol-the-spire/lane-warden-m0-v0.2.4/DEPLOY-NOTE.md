# Deployment note

M0-0.2.4 is an immutable ATT-001 UX test build. R01-C tuning is frozen from v0.2.3. `main.js` and `rules.js` remain byte-identical to v0.2.3; a verified loader injects only camera observation/focus references needed by the world-locked danger indicator. Simulation rules and parameter values are unchanged.
