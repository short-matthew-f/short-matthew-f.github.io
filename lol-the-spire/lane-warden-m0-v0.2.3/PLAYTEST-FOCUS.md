# v0.2.3 human-test focus

Run **Siege + delay at 1×**.

This is a pacing/consequence test, **not** an independent-teaching test. The player already knows the Twin Toll thesis, so do not use this run to claim that the encounter teaches itself unaided.

Observe:

1. Does the battle now feel like multiple meaningful macro cycles rather than a ~3-minute sprint?
2. Does North spend enough time visibly making real Guard progress before the breach converts?
3. When South Bastion falls, does the remaining Core clock feel genuinely dangerous rather than decorative?
4. Does the longer battle preserve the same `sacrifice / Guard read / gold fork` clarity from v0.2.2?
5. Does the unchanged tap-through HUD remain easy to use for the longer duration?

The evidence now records `fork-open` and `intervention-ready` transitions through the isolated DL-001 instrumentation layer. Export after the battle.
